<Window x:Class="WpfApp1.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="login" Height="250" Width="400" Closed="Window_Closed">
    <Grid>
        <Menu/>
        <StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="Логин:" FontWeight="Bold" FontSize="20" Foreground="#FF5E7279" Margin="0 0 10 0" Width="80"/>
                <TextBox x:Name="boxLogin" Width="197" Height="30" FontSize="14" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="Пароль:" FontWeight="Bold" FontSize="20" Foreground="#FF5E7279" Margin="0 0 10 0"/>
                <TextBox x:Name="boxPassword" Width="200" Height="30" FontSize="14" />
            </StackPanel>
            <Button  Content="Вход" HorizontalAlignment="Center" Margin="0 20" Width="120" Height="30" Background="#FF5E7279" 
                    Foreground="White" FontSize="16" FontWeight="Bold" Click="btnCheck" />
        </StackPanel>
    </Grid>
</Window>

